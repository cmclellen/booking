import styled from 'styled-components';

export const HeaderWrapper = styled.div`
`;

export const Brand = styled.a`
   font-family: RubikDoodleShadow, serif;
`;

export const AppTitle = styled.small`
   font-family: CarterOne, serif;
`;